[
  {
    "id": "eds-array-basics",
    "title": "Array — Định nghĩa & Sieve of Eratosthenes",
    "content": "- **Array**: tập hợp cố định các phần tử **cùng kiểu**, lưu **liền nhau** trong bộ nhớ, truy cập qua index — $O(1)$.\n- **Dynamic array**: kích thước thay đổi được trong khi chương trình chạy.\n- **Ví dụ Sieve of Eratosthenes** — tìm mọi số nguyên tố < $N$:\n```cpp\nvoid sieve(int N) {\n    int *a = new int[N];\n    for (int i = 2; i < N; i++) a[i] = 1;\n    for (int i = 2; i < N; i++)\n        if (a[i])\n            for (int j = i; i*j < N; j++) a[i*j] = 0;\n    for (int i = 2; i < N; i++)\n        if (a[i]) cout << \" \" << i;\n    delete[] a;\n}\n```\n- Độ phức tạp Sieve: $O(N \\log \\log N)$ thời gian, $O(N)$ không gian.",
    "recallPrompt": "Giải thích tại sao vòng lặp trong của Sieve bắt đầu với `j = i` (tức `i*i`) thay vì `j = 2` — điều gì đã được đánh dấu trước đó rồi?",
    "blanks": ["liền nhau", "dynamic array", "$O(N \\log \\log N)$"]
  },
  {
    "id": "eds-linkedlist-node",
    "title": "Singly Linked List — Cấu trúc ListNode",
    "content": "- **Linked list**: tập hợp node, mỗi node chứa **data** và **link** (pointer) đến node kế tiếp.\n- Cấu trúc node cơ bản:\n```cpp\nstruct ListNode {\n    DataType data;\n    ListNode *next;\n    ListNode(DataType data, ListNode *next=nullptr) {\n        this->data = data;\n        this->next = next;\n    }\n};\ntypedef ListNode *Link;\n```\n- **Null link**: trỏ đến không node nào — đánh dấu cuối danh sách.\n- **Dummy head node**: node đầu không chứa data thực — đơn giản hoá các thao tác chèn/xoá.\n- Tạo node: `ListNode *p = new ListNode(...);`\n- Xoá node: `delete p;` — chú ý dangling pointer sau khi xoá.",
    "recallPrompt": "Giải thích mục đích của dummy head node — thao tác chèn vào đầu danh sách trở nên đơn giản hơn như thế nào so với danh sách không có dummy node?",
    "blanks": ["ListNode", "null link", "dummy head node"]
  },
  {
    "id": "eds-linkedlist-structure",
    "title": "LinkedList — Cấu trúc Quản lý & Duyệt",
    "content": "- **Cấu trúc cơ bản** (chỉ `first`): phù hợp thêm đầu, xoá đầu $O(1)$; thêm cuối $O(n)$:\n```cpp\nstruct LinkedList {\n    ListNode *first;  // head\n    LinkedList() { this->first = nullptr; }\n};\n```\n- **Cấu trúc mở rộng** (có `first` và `last`): thêm cuối $O(1)$:\n```cpp\nstruct LinkedList {\n    ListNode *first;\n    ListNode *last;\n    LinkedList() { this->first = nullptr; this->last = nullptr; }\n};\n```\n- **Duyệt** (traversal):\n```\nAssign head to current pointer.\nwhile current != null:\n    process current->data\n    current = current->next\n```\n- **Tìm kiếm** — $O(n)$:\n```cpp\nListNode *search(ListNode *first, DataType k) {\n    ListNode *current = first;\n    while (current) {\n        if (current->data == k) return current;\n        current = current->next;\n    }\n    return nullptr;\n}\n```",
    "recallPrompt": "Tại sao thêm `last` pointer vào LinkedList giúp insert-at-end từ $O(n)$ xuống $O(1)$ — nhưng xoá cuối vẫn là $O(n)$?",
    "blanks": ["first", "last", "$O(1)$"]
  },
  {
    "id": "eds-linkedlist-ordered",
    "title": "Ordered Linked List — Chèn Có Thứ tự",
    "content": "- **Ordered linked list**: các phần tử duy trì thứ tự tăng (hoặc giảm).\n- **Thuật toán chèn**:\n```\nCreate new node with data.\nif list is empty:\n    new node becomes first.\nelse:\n    Find first node >= new value (or end of list).\n    Insert new node before found node (or at end).\n```\n- Độ phức tạp chèn: $O(n)$ — phải duyệt để tìm vị trí.\n- **Lợi ích dummy head**: loại bỏ special case khi chèn vào đầu — code xử lý mọi vị trí như nhau.",
    "recallPrompt": "Giải thích tại sao dùng dummy head node trong ordered linked list giúp tránh if-else riêng cho trường hợp chèn vào đầu danh sách.",
    "blanks": ["ordered linked list", "$O(n)$", "dummy head"]
  },
  {
    "id": "eds-linkedlist-variations",
    "title": "Biến thể Linked List — Circular, Doubly, Generalized",
    "content": "- **Circular linked list**: node cuối trỏ về node đầu — không có node nào có `next = nullptr`. Phù hợp Josephus problem.\n- **Doubly linked list**: mỗi node có **hai pointer**: `next` và `prev`. Cho phép duyệt 2 chiều; xoá node $O(1)$ nếu có pointer đến node đó.\n- **Generalized list**: dãy hữu hạn phần tử, mỗi phần tử có thể là **giá trị** hoặc **sub-list** (lồng nhau đệ quy):\n```cpp\nstruct GenListNode {\n    bool tag;          // false = atom, true = sublist\n    GenListNode* next;\n    union {\n        DataType data;     // nếu tag = false\n        GenListNode* down; // nếu tag = true\n    };\n};\n```\n- **Multi-linked list**: mỗi node có nhiều pointer — biểu diễn cấu trúc phân cấp.",
    "recallPrompt": "Giải thích trường `tag` và `union` trong `GenListNode` — tại sao cần `tag` khi đã dùng `union`?",
    "blanks": ["Circular", "Doubly", "GenListNode"]
  },
  {
    "id": "eds-stack-definition",
    "title": "Stack — LIFO, API & Ứng dụng",
    "content": "- **Stack**: lưu trữ và lấy phần tử theo chính sách **LIFO** (Last-In, First-Out).\n- **API**:\n\n| Method | Mô tả |\n|---|---|\n| `isEmpty()` | Stack rỗng? |\n| `size()` | Số phần tử |\n| `push(item)` | Thêm vào đỉnh — $O(1)$ |\n| `top()` | Xem đỉnh, không xoá — $O(1)$ |\n| `pop()` | Xoá đỉnh — $O(1)$ |\n\n- **Ứng dụng thực tế**:\n  - Parsing trong compiler.\n  - Function call stack (push env khi gọi, pop khi return).\n  - Undo/redo trong editor.\n  - Back button trong browser.\n  - **Loại bỏ đệ quy**: mọi hàm đệ quy đều có thể viết lại bằng explicit stack.",
    "recallPrompt": "Giải thích cơ chế compiler dùng stack để implement function calls — điều gì được push/pop tại thời điểm call và return?",
    "blanks": ["LIFO", "push", "pop"]
  },
  {
    "id": "eds-stack-arithmetic",
    "title": "Stack — Biểu thức Arithmetic: Infix, Prefix, Postfix",
    "content": "- **3 dạng biểu thức**:\n  - **Infix**: `⟨op1⟩ ⟨operator⟩ ⟨op2⟩` — cần ngoặc/ưu tiên.\n  - **Prefix (Polish)**: `⟨operator⟩ ⟨op1⟩ ⟨op2⟩` — không cần ngoặc.\n  - **Postfix (RPN)**: `⟨op1⟩ ⟨op2⟩ ⟨operator⟩` — dễ tính bằng stack.\n\n| Infix | Prefix | Postfix |\n|---|---|---|\n| `A+B*C` | `+A*BC` | `ABC*+` |\n| `(A+B)*(C-D)` | `*+AB-CD` | `AB+CD-*` |\n\n- **Tính postfix bằng stack** (Dijkstra two-stack algorithm cho fully-parenthesized infix):\n  - Gặp số: push vào value stack.\n  - Gặp operator: push vào operator stack.\n  - Gặp `)`: pop operator + 2 values, tính, push kết quả.",
    "recallPrompt": "Tính bằng tay giá trị của biểu thức postfix `5 3 + 2 *` bằng stack — mô tả trạng thái stack sau mỗi token.",
    "blanks": ["Postfix", "two-stack", "push"]
  },
  {
    "id": "eds-stack-infix-to-postfix",
    "title": "Stack — Chuyển Infix sang Postfix",
    "content": "- **Thuật toán** (dùng operator stack):\n```\nstackOps.push('(')\ninfixExp.append(')')\nwhile not infixExp.end():\n    tok = infixExp.nextToken()\n    if tok is operand:  postfixExp.append(tok)\n    if tok is '(':      stackOps.push(tok)\n    if tok is operator:\n        while precedence(stackOps.top()) >= precedence(tok):\n            postfixExp.append(stackOps.pop())\n        stackOps.push(tok)\n    if tok is ')':\n        while stackOps.top() != '(':\n            postfixExp.append(stackOps.pop())\n        stackOps.pop()  // bỏ '('\n```\n- **Quy tắc precedence**: `*`, `/` > `+`, `-`.\n- Ví dụ: `(A+B)*(C-D)` → `AB+CD-*`.",
    "recallPrompt": "Trace thuật toán chuyển `A+B*C` sang postfix — tại sao `*` được pop trước `+` khi gặp cuối biểu thức?",
    "blanks": ["operator stack", "precedence", "postfixExp"]
  },
  {
    "id": "eds-queue-definition",
    "title": "Queue — FIFO, API & Ứng dụng",
    "content": "- **Queue**: lưu trữ và lấy phần tử theo chính sách **FIFO** (First-In, First-Out).\n- **API**:\n\n| Method | Mô tả |\n|---|---|\n| `isEmpty()` | Queue rỗng? |\n| `enqueue(item)` | Thêm vào cuối — $O(1)$ |\n| `dequeue()` | Xoá ở đầu — $O(1)$ |\n| `front()` | Xem phần tử đầu — $O(1)$ |\n\n- **Ứng dụng**: OS (queuing IO requests, mouse events), web server (incoming requests), hàng đợi ngân hàng, quầy vé.\n- **Khác Stack**: Stack lấy phần tử *mới nhất*, Queue lấy phần tử *cũ nhất*.",
    "recallPrompt": "Giải thích tại sao một hệ điều hành cần Queue (không phải Stack) để xử lý IO requests — điều gì xảy ra với fairness nếu dùng Stack?",
    "blanks": ["FIFO", "enqueue", "dequeue"]
  },
  {
    "id": "eds-deque-definition",
    "title": "Deque — Double Ended Queue",
    "content": "- **Deque** (Double Ended Queue): thêm và xoá ở **cả hai đầu**.\n- **API**:\n\n| Method | Mô tả |\n|---|---|\n| `push_front(item)` | Thêm vào đầu |\n| `push_back(item)` | Thêm vào cuối |\n| `pop_front()` | Xoá ở đầu |\n| `pop_back()` | Xoá ở cuối |\n\n- **Deque là tổng quát hoá** của Stack và Queue:\n  - `push_back` + `pop_back` = **Stack** (LIFO)\n  - `push_back` + `pop_front` = **FIFO Queue**\n- **Array-based deque**: dùng circular buffer để đảm bảo $O(1)$ cho mọi thao tác.",
    "recallPrompt": "Giải thích tại sao dùng array thông thường (non-circular) làm deque gặp vấn đề $O(n)$ khi pop_front — circular buffer giải quyết điều này như thế nào?",
    "blanks": ["Double Ended Queue", "push_front", "circular buffer"]
  },
  {
    "id": "eds-symboltable-adt",
    "title": "Symbol Table ADT — Key-Value Abstraction",
    "content": "- **Symbol table**: cấu trúc dữ liệu lưu cặp **(key, value)**, hỗ trợ:\n  - `insert(key, value)`: thêm cặp mới.\n  - `search(key)`: tìm value theo key.\n  - `remove(key)`: xoá cặp.\n- **Ứng dụng**: DNS lookup, compiler (biến → type), dictionary, routing table, file system.\n- **Quy ước**: keys là Comparable, unique, không null. Values không null.\n- **Ordered Symbol Table** — thêm các phương thức:\n  - `min()`, `max()`: key nhỏ nhất/lớn nhất.\n  - `floor(key)`: key lớn nhất ≤ key.\n  - `ceiling(key)`: key nhỏ nhất ≥ key.\n  - `rank(key)`: số keys < key.\n  - `select(k)`: key có rank k.",
    "recallPrompt": "Giải thích sự khác biệt giữa `floor(key)` và `ceiling(key)` trong Ordered Symbol Table — dùng ví dụ với set keys = {1, 3, 5, 7} và query key = 4.",
    "blanks": ["key-value", "floor", "rank"]
  },
  {
    "id": "eds-symboltable-implementations",
    "title": "Symbol Table Implementations — So sánh Độ phức tạp",
    "content": "- **Unordered list**: search $O(N)$, insert $O(1)$, remove $O(N)$. Không hỗ trợ ordered iteration.\n- **Ordered list**: search $O(N)$, insert $O(N)$, remove $O(N)$. Có ordered iteration.\n- **Ordered array + Binary Search**: search $O(\\log_2 N)$, insert $O(N)$ (phải dời), remove $O(N)$.\n- **Sequential search** — $N/2$ compares trung bình:\n```cpp\nValue seqsearch(int l, int r, Key key) {\n    for (int i = l; i <= r; i++)\n        if (keys[i] == key) return values[i];\n    return nullValue;\n}\n```\n- **Binary search** — $\\leq \\log_2(N+1)$ compares:\n```cpp\nValue binsearch(int l, int r, Key key) {\n    do {\n        int m = (l + r) / 2;\n        if (keys[m] == key) return values[m];\n        else if (keys[m] > key) r = m - 1;\n        else l = m + 1;\n    } while (l <= r);\n    return nullValue;\n}\n```\n- **Interpolation search**: thay $m = l + \\frac{key - keys[l]}{keys[r] - keys[l]}(r-l)$ — tốt hơn binary search khi keys phân phối đều.",
    "recallPrompt": "Tại sao ordered array có search $O(\\log N)$ nhưng insert lại $O(N)$ — bottleneck nằm ở đâu và cấu trúc nào tiếp theo sẽ giải quyết điều này?",
    "blanks": ["$O(\\log_2 N)$", "binary search", "interpolation search"]
  },
  {
    "id": "eds-quickselect",
    "title": "Selection — Tìm Phần tử Thứ k (Quickselect)",
    "content": "- **Bài toán**: tìm phần tử nhỏ thứ $k$ trong mảng mà **không cần sắp xếp hoàn toàn**.\n- **Quickselect** (dựa trên partition của Quicksort):\n```cpp\ntemplate <class Item>\nvoid select(Item a[], int l, int r, int k) {\n    if (r <= l) return;\n    int i = partition(a, l, r);\n    if (i > k) select(a, l, i-1, k);\n    if (i < k) select(a, i+1, r, k);\n    // nếu i == k: a[k] là phần tử cần tìm\n}\n```\n- Độ phức tạp **trung bình**: $O(n)$ — tốt hơn sort $O(n \\log n)$ rất nhiều.\n- Độ phức tạp **worst case**: $O(n^2)$ (cùng vấn đề với Quicksort).",
    "recallPrompt": "Giải thích tại sao Quickselect đạt $O(n)$ trung bình — sau mỗi partition, bao nhiêu phần của mảng bị loại bỏ so với Quicksort?",
    "blanks": ["partition", "$O(n)$", "Quickselect"]
  }
]